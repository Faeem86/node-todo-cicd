<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Todo List APP - Dark & Fabulous Edition 9 July 2024</title>
        <style>
            body {
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                background: linear-gradient(to right, #0f2027, #203a43, #2c5364);
                color: #f5f5f5;
                margin: 0;
                padding: 0;
                display: flex;
                flex-direction: column;
                align-items: center;
            }

            h1 {
                background: linear-gradient(to right, #ff416c, #ff4b2b);
                color: white;
                margin: 0;
                padding: 20px;
                text-align: center;
                width: 100%;
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
                text-transform: uppercase;
            }

            ul {
                list-style: none;
                padding: 0;
                width: 80%;
                max-width: 600px;
            }

            ul li {
                background: #333;
                border-radius: 8px;
                border: 1px solid #555;
                margin-bottom: 10px;
                padding: 10px;
                position: relative;
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
                transition: transform 0.2s ease-in-out, background 0.2s ease-in-out;
            }

            ul li:hover {
                transform: scale(1.02);
                background: #444;
            }

            ul li a {
                color: #f5f5f5;
                text-decoration: none;
                font-weight: bold;
            }

            ul li a:hover {
                color: #ff416c;
            }

            .delete-btn {
                color: #ff4b2b;
                position: absolute;
                right: 10px;
                top: 10px;
                font-size: 20px;
                text-decoration: none;
            }

            .edit-btn {
                color: #FFC107;
                position: absolute;
                right: 40px;
                top: 10px;
                font-size: 20px;
                text-decoration: none;
            }

            form {
                background: #333;
                padding: 20px;
                margin-top: 20px;
                border-radius: 8px;
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
                width: 80%;
                max-width: 600px;
            }

            label {
                display: block;
                margin-bottom: 10px;
                font-size: 18px;
                font-weight: bold;
            }

            input[type="text"] {
                width: 100%;
                padding: 10px;
                margin-bottom: 10px;
                border: 1px solid #555;
                border-radius: 4px;
                background: #444;
                color: #f5f5f5;
            }

            input[type="submit"] {
                background-color: #ff416c;
                color: white;
                border: none;
                padding: 10px 20px;
                cursor: pointer;
                border-radius: 4px;
                transition: background-color 0.2s ease-in-out;
            }

            input[type="submit"]:hover {
                background-color: #ff4b2b;
            }
        </style>
    </head>

    <body>
        <h1>Todo List - Dark & Fabulous Edition</h1>
        <ul>
            <% todolist.forEach(function(todo, index) { %>
            <li>
                <a href="/todo/delete/<%= index %>" class="delete-btn">✘</a>
                <a href="/todo/<%= index %>" class="edit-btn">✎</a>
                <%- todo %>
            </li>
            <% }); %>
        </ul>

        <form action="/todo/add/" method="post">
            <label for="newtodo">What should I do?</label>
            <input type="text" name="newtodo" id="newtodo" autofocus />
            <input type="submit" value="Add" />
        </form>
    </body>
</html>
